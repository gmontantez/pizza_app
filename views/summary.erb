<!DOCTYPE html>
<html>
<head>
	<title></title>
<style>

</style>
</head>
<body class="background4">
<h2><p class="d">Thank you for your selections!</p></h2>
<h2><p class="c">You chose a <%=size%> pizza with <%=sauce%> sauce and a <%=crust%> crust for $<%=('%.2f' % session[:size_choice].to_f)%>.</p></h2>
<%if session[:meat] != "" && session[:veggie] != "" && session[:other] != "" && session[:other_options] != ""%> 
<h2><p class="c">Your toppings are: <%=meat.join(', ')%>, <%=veggie.join(', ')%>, <%=other.join(', ')%>, and <%=session[:other_options].join(', ')%> for $<%=('%.2f' % session[:other_options_choice].to_f)%></p></h2>
<%end%>
<h2>-----------------------------------------<br></h2>
<h2><p class="f">Please confirm your choices below:</p></h2>

<form method="post" action="summary">
<h2><p class="f"><%meat.each do |choice| %>
    	<%if meat == nil%>
    	<%else%>
    <%=choice%><input type="radio" name="meat[<%=choice%>]" value="<%=choice%>">Yes 
    <input type="radio" name="meats[<%=choice%>]" value = "">No<br>
    <%end%>
<%end%>
-----------------------------------------<br>
<%veggie.each do |choice| %>
    <%if veggie == nil%>
    <%else%>
    <%=choice%><input type="radio" name="veggie[<%=choice%>]" value="<%=choice%>">Yes 
    <input type="radio" name="veggies[<%=choice%>]" value="">No<br>
    <%end%>
<%end%>
-----------------------------------------<br>
<%other.each do |choice| %>
    <%if other == nil%>
    <%else%>
    <%=choice%><input type="radio" name="other[<%=choice%>]" value="<%=choice%>">Yes 
    <input type="radio" name="others[<%=choice%>]" value="">No<br>
    <%end%>
<%end%>
-----------------------------------------<br>
<%session[:other_options].each do |choice| %>
    <%if choice == nil%>
    <%else%>
    <%=choice%><input type="radio" name="other_options[<%=choice%>]" value="<%=choice%>">Yes 
    <input type="radio" name="others_options[<%=choice%>]" value="">No<br>
-----------------------------------------<br>
    <%end%>
<%end%>
</p>
</h2>
</form>
<h2><p class="i">Please select "Redo Choices" if you want to remake your pizza:
<form method="get" action="redo">
	<input type="submit" value ="Redo Choices">
</form></p></h2>
<form method="post" action="summary">
<h2><p class="f">Select "Add Another Pizza" and "Submit" if you'd like to order another pizza:</p>
<p class="k"><input type="checkbox" name="add_another_pizza" value="More">Add Another Pizza</p>
	<input type="submit"></h2>
<h2><p class="e">Would you like your order delivered?</p>
<p class="g"><input type="radio" name="delivery" value="Yes">Yes</p>
<p class="i"><input type="radio" name="delivery" value="No">No</p>
	<input type="submit"></p></h2>
</form>



</center>
</body>
</html>


